*.o

# cmake build
build

#[IDE] CLion IDE, CMake build
/build-clion/

# symlink to assets
mingw-linux/assets

# Android APK build lives in /android and its project files are tracked;
# only the downloaded SDL2 source and build outputs are ignored — see
# android/.gitignore. Built APKs stay untracked.
*.apk

# https://www.codeblocks.org/
*.layout
*.depend
/cb-mswindows/bin
/cb-linux/bin

# built via make, but not tracked in git (root only: android/app/ is a
# tracked Gradle module)
/app
